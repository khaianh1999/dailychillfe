export default function ({ route, app, redirect }) {
    // const protectedRoutes = ['/profile', '/list-my-order']; // ← danh sách route cần bảo vệ
    // console.log("app.$cookies", app.$cookies);
    // if (protectedRoutes.includes(route.path)) {
    //   const token = app.$cookies.get('token_user');
    //   const user = app.$cookies.get('user_infor');
  
    //   if (!token || !user) {
    //     return redirect('/');
    //   }
    // }
}